/**
 * variable a dan function b bukan berada di dalam function
 * maka mereka akan berada di dalam global object
 * Untuk testing di console log :
 * - a
 * - window.a
 */

var a = "Hello World";

function b() {
  
}